# Changelog

## [0.9.0] - 2026-09-15

### Fixed
- **インタースティシャル広告が一度も表示されていなかったバグを修正**
  - `showInterstitialAd(completion:)` の呼び出し元がコードベース内にゼロだった。
    詳細画面を開くたびにロードは走っていた（月47リクエスト・充填100%）が、
    表示するコードが無く、ロード済み広告が毎回破棄されていた
  - 発火点を「AIノート生成の開始時」に接続。オンデバイス生成の待ち時間が
    そのまま広告の尺になるため、体験を止めない
  - 録音中・文字起こし処理中は表示しないガードを追加
  - 広告の presenter 解決を修正（非アクティブシーン／シート表示中に表示が失敗していた）
- **Info.plist の `GADApplicationIdentifier` が Google のテスト用アプリIDのまま
  出荷されていた問題を修正**。本番アプリIDに差し替え
- `Prod.xcconfig` が Release configuration にしか紐づいておらず、
  Debug ビルドで広告ユニットIDが空になっていた設定バグを修正

### Added
- **アプリ起動（App Open）広告**
  - ロード待ち上限5秒（未ロードで即諦める実装は起動時に間に合わず表示率が激減するため）
  - 起動5回に1回の表示ゲート。カウンタは広告専用キー
  - 録音中・文字起こし中は表示しない（背景録音からの復帰時に画面へかぶらないように）
- 全画面広告の相互排他（`AdPresentationCoordinator`）。
  インタースティシャルとアプリ起動広告が連続2枚出ないことを構造的に担保
- テスト用広告IDがリリースビルドに混入しないためのガード（コンパイル時・実行時・テストの3層）

### 答え合わせキー（リリース+7日で確認）
- `movie`（interstitial）: 表示率 **0%（47リクエスト→0表示）** から動くか。imp > 0 が最低ライン
- `app_open`: 新規フォーマット。表示率が他プロダクトの失敗値 **0.2%** を大きく上回るか
  （ロード待ち5秒が効いているかの検証）
- 本番アプリID差し替え後、`all_banner` の充填率・eCPM（445円）に変化があるか
- 前提: バナーのリクエストが月107回＝ユーザー数自体が少ないため、
  金額の絶対値は小さいまま。**見るのは金額ではなく表示率**

## [0.8.0] - 2026-08-24

### Added
- AIノート機能（オンデバイス生成）
  - 文字起こし結果から「要約」と「アクションアイテム」をワンタップで生成
  - Apple FoundationModels によるオンデバイス推論。音声・文字起こしを外部送信しない
  - Apple Intelligence 非対応端末では専用の案内を表示
  - 生成結果は Core Data の `aiTranscriptionText` に永続化
- ストア用スクリーンショットの自動生成の仕組み

### Fixed
- `fetchVoiceMemo(id:)` が保存済みAIノートを復元しないバグを修正

### Changed
- ストア文言を実際の機能に合わせて是正し、AIノートの説明を追記

## [Unreleased] - 次回リリース予定

### Added
- SpeechAnalyzer API対応 (iOS 26+)
  - Apple純正の高精度オンデバイス文字起こし
  - iOS 26+: SpeechAnalyzerService使用
  - iOS 25以下: WhisperKit使用（従来通り）
  - TranscriptionServiceProtocolによる統一インターフェース

## [0.2.0] - 2025-XX-XX

### Added
- 文字起こし結果コピー機能
- No Speech detectedエラーハンドリング改善
- バナー広告を全画面に追加
- AdMob環境値システム実装
- タイトル編集モーダル化
- AI文字起こし前広告表示機能
